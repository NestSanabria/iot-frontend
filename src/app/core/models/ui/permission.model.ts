// src/app/core/models/permission.model.ts

/**
 * Modelo de permiso usado en el frontend (tablas, asignaciones, etc.).
 */
export interface PermissionModel {
  permissionId: string;
  permissionName: string;
  permissionDescription: string;
}
